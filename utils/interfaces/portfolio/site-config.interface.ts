export interface ISiteConfig {
  name: string;
  /** Full display name — set as the masthead on the landing page. */
  fullName: string;
  url: string;
  title: string;
  tagline: string;
  bio: string[];
  email: string;
  github: string;
  linkedin: string;
  facebook: string;
  instagram: string;
  resume: string;
}
